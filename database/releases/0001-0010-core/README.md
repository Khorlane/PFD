# PFD PostgreSQL Core Build SQL Package

This release is the executable database foundation for \<business name>. It translates the approved persistent-data architecture into a controlled PostgreSQL build without introducing surrogate keys or a separate simulation copy of the business.

Start with [`database/README.md`](database/README.md). That guide covers prerequisites, role and database bootstrap, the validated build runner, verification, tests, and operational safeguards.

Release: `1.0.0`  
Published: September 4, 2026  
Final change: `0010`

The package does not contain passwords, connection strings, production data, or destructive production-database commands.
